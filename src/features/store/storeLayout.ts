/** Grid metrics shared by the store pages so rows page to the viewport. */
export const CARD_ART = 100;
export const CARD_GAP = 18;
/** Artwork + name + meta + action row. */
export const CARD_HEIGHT = 170;
export const SECTION_HEADER = 30;
